<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="demo.great.dao.mapper.UserMapper">
    
    <resultMap id="BaseResultMap" type="demo.great.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="status" property="status" />
    </resultMap>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>
    
    <insert id="insert" parameterType="demo.great.entity.User">
        INSERT INTO user (username, password, email, phone, status, create_time)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{status}, NOW())
    </insert>
    
</mapper>